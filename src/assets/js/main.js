import stars from './blocks/stars';

window.addEventListener('DOMContentLoaded', () => {
    stars('.testimonials__carousel_track_item_descr_stars_star', 
           '.testimonials__carousel_track_item_descr_stars'
        );
});